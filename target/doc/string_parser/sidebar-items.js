initSidebarItems({"fn":[["string_parser","Arguments * `path` - the path to the file to search from * `text` - the text to search * `end_filter` - the function called at each character to check if we're still within the token. Sould return true when out of the token. * `callback` - the function called when the text is exited. take the inside of the token as argument # Examples ./text being \"...'foo'...\" ```no_run extern crate string_parser; use string_parser::string_parser;"]]});